const message = 'Hello  Morning#@'

const uppercase = message.toUpperCase()
const lowercase = message.toLowerCase()

const faltuMessage ='    Hi, This is Sandeep Kumar     '

const finalMessage = faltuMessage.trim()
// console.log(finalMessage.replaceAll('Sandeep','Pandit'))

const finalDigit = '9720'

const templateString = `Hello Where are you , ${message}`